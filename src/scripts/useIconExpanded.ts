/**
 * Utility function to easily toggle between the downwards and upwards
 * caret icons.
 */
export default function(expanded: boolean) {
    return expanded ? "mdi-chevron-up" : "mdi-chevron-down";
}
